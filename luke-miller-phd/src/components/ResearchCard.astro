---
interface Props {
  title: string;
  date: Date;
  category: string;
  tags: string[];
  summary: string;
  slug: string;
}

const { title, date, category, summary, slug } = Astro.props;

const formattedDate = date.toLocaleDateString('en-US', {
  year: 'numeric',
  month: 'long',
});
---

<article class="group border border-lm-border rounded-sm bg-lm-surface hover:border-lm-accent/50 transition-colors duration-200">
  <a href={`/research/${slug}`} class="block px-6 py-5" aria-label={`Read: ${title}`}>
    <div class="flex items-center gap-3 font-mono text-xs text-lm-muted tracking-wider uppercase mb-3">
      <time datetime={date.toISOString()}>{formattedDate}</time>
      <span aria-hidden="true">&middot;</span>
      <span class="text-lm-accent">{category}</span>
    </div>
    <h3 class="font-serif text-base text-lm-text group-hover:text-lm-accent transition-colors duration-150 mb-2 leading-snug">
      {title}
    </h3>
    <p class="text-sm text-lm-muted leading-relaxed line-clamp-2">{summary}</p>
  </a>
</article>
