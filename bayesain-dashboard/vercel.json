{
  "version": 2,
  "buildCommand": "npm install && cd client && npm install && npm run build && mkdir -p ../public/app && cp -r dist/* ../public/app/",
  "outputDirectory": "public",
  "functions": {
    "api/index.js": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" },
    { "source": "/:path*", "destination": "/:path*" }
  ]
}
