{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd bayesain-dashboard && npm install && npm run build:client && npx puppeteer browsers install chrome"
  },
  "deploy": {
    "startCommand": "cd bayesain-dashboard && node server/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
